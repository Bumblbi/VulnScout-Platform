### **🛡️ VulnScout Platform**
**VulnScout Platform** — это комплексная система для оценки безопасности информационных систем с использованием современных инструментов сканирования и анализа уязвимостей.

## **🌟 Особенности**
  · **Многоуровневое сканирование** — сеть, веб-приложения, сервисы
  · **Автоматическое обнаружение уязвимостей** — интеграция с CVE базами и БДУ ФСТЭК России
  · **Модуль имитации атак** — проверка на SQL-инъекции, подбор учетных данных
  · **Построение векторов атак** — анализ цепочек уязвимостей
  · **Детальная отчетность** — JSON, HTML, PDF форматы
  · **Асинхронная обработка** — Celery для фоновых задач сканирования
  · **RESTful API** — полная интеграция с внешними системами

## **🏗️ Архитектура**

VulnScout Platform/
├── FastAPI Backend      # REST API сервер
├── Celery Workers       # Асинхронные задачи сканирования  
├── MySQL Database       # Хранение результатов
├── Redis                # Брокер сообщений
└── Modern Frontend      # React/Vue.js интерфейс (в разработке)

## **🚀 Быстрый старт**

# **Предварительные требования**

  · Python 3.9+
  · MySQL 8.0+
  · Redis 6.0+
  · Nmap
  · SQLMap (опционально)

# **Установка**

1. **Клонирование репозитория**
```
git clone https://github.com/Bumblbi/VulnScout-Platform.git
cd VulnScout-Platform
```

2. **Настройка виртуального окружения**

```
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. **Установка зависимостей**

```
pip install -r requirements.txt
```

4. **Настройка базы данных**

```
# Создайте базу данных MySQL
mysql -u root -p -e "CREATE DATABASE security_scanner;"

# Обновите настройки в config/settings.py
DATABASE_URL = "mysql+pymysql://user:password@localhost/security_scanner"
```

5. **Инициализация БД**

```
python -c "from app.database import init_db; init_db()"
```

# **Запуск**

1. **Запуск Redis**

```
redis-server
```

2. **Запуск Celery Worker**

```
celery -A app.celery_app.celery_worker.celery_app worker --loglevel=info
```

3. **Запуск FastAPI сервера**

```
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

4. **Откройте в браузере**

```
http://localhost:8000/docs
```

## **📚 API Документация**
# **Основные эндпоинты**

**Запуск сканирования**
```
POST /api/v1/scans/
Content-Type: application/json

{
  "target": "example.com",
  "scan_type": "full",
  "options": {}
}
```
**Получение статуса сканирования**
```
GET /api/v1/scans/{scan_id}
```
**Получение уязвимостей**
```
GET /api/v1/vulnerabilities/?severity=high
```
**Генерация отчетов**
```
GET /api/v1/reports/{scan_id}?format=json|html|pdf
```

# **Типы сканирования**
  · ```network``` — только сетевое сканирование
  · ```web``` — сканирование веб-приложений
  · ```full``` — полное сканирование
  · ```custom``` — пользовательская конфигурация

## **🔧 Модули сканирования**
# **Сетевой сканер**

  · Обнаружение хостов и сервисов
  · Определение версий ПО
  · Анализ открытых портов
  · Использует: ```python-nmap```

# **Сканер уязвимостей**

  · Проверка CVE уязвимостей
  · Интеграция с БДУ ФСТЭК России
  · Анализ версий ПО на уязвимости
  · Использует: NVD API, ФСТЭК API

# **Модуль имитации атак**

  · Подбор учетных данных (SSH, FTP, HTTP)
  · Проверка на SQL-инъекции
  · Поиск мисконфигураций
  · Использует: ```paramiko```, ```sqlmap```

# **Анализатор атак**

  · Построение графов атак
  · Выявление цепочек уязвимостей
  · Оценка рисков
  · Использует: networkx

## **📊 Пример отчета**
```
{
  "scan_id": 1,
  "target": "192.168.1.0/24",
  "status": "completed",
  "summary": {
    "hosts_scanned": 12,
    "vulnerabilities_found": 45,
    "critical": 3,
    "high": 12,
    "medium": 20
  },
  "attack_vectors": [
    {
      "source": "SSH bruteforce on 192.168.1.10",
      "target": "CVE-2021-44228 on 192.168.1.15",
      "confidence": 0.85
    }
  ]
}
```

## **🛠️ Технологический стек**
# **Backend**

  · FastAPI — современный ASGI фреймворк
  · SQLAlchemy — ORM для работы с БД
  · Celery — асинхронные задачи
  · Pydantic — валидация данных

# **База данных**

  · MySQL — основное хранилище
  · Redis — кэширование и брокер сообщений

# **Инструменты безопасности**

  · Nmap — сетевой сканер
  · SQLMap — тестирование на SQL-инъекции
  · Python-nmap — Python обертка для Nmap

## **🤝 Разработка**
# **Структура проекта**
```
app/
├── api/           # FastAPI эндпоинты
├── models/        # SQLAlchemy модели
├── scanners/      # Модули сканирования
├── celery_app/    # Celery задачи
├── schemas/       # Pydantic схемы
└── utils/         # Вспомогательные функции
```

# **Добавление нового модуля сканирования**
  1. Создайте файл в ```app/scanners/```
  2. Реализуйте класс сканера
  3. Добавьте импорт в ```app/scanners/__init__.py```
  4. Интегрируйте в ```app/celery_app/tasks.py```

## **👥 Команда разработки**
  · **Backend**: Александр (Bumblbi)
  · **Frontend**: Глеб (glebsmirnov200629-sketch)
  · **FullStack**: Александр (SanoEnger)
